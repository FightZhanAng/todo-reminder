import type { JSX } from 'react'
import type { AppState } from '../useAppState'

/** 桩：提示条的渲染留待后续任务接入（界面骨架阶段先放最小桩） */
export function NoticeBar(_props: { state: AppState }): JSX.Element | null {
  return null
}

import type { JSX } from 'react'
import type { AppState } from '../useAppState'

/** 桩：Task 11 换成真正的设置面板 */
export function SettingsPanel(_props: { state: AppState }): JSX.Element | null {
  return null
}
